import create from 'zustand';


const useStore = create(set => ({
    userId: "",
    userToken: "",
    setUserId: (incomingId: string) => set(state => ({userId: incomingId })),
    setUserToken: (incomingToken: string) => set(state => ({userToken: incomingToken})),
}))

